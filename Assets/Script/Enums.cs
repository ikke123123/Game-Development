public enum Team { red, blue };

public enum CharacterType { assassin, archer, tank };

public enum VerticalDirection { unspecified, up, down };

public enum HorizontalDirection { unspecified, left, right };

public enum Ability { one, two, three };